{
  "name": "pgats-api",
  "version": "1.0.0",
  "description": "Esta API permite o registro, login, consulta de usuários e transferências de valores entre usuários, com regras de negócio para aprendizado de testes e automação de APIs.",
  "main": "app.js",
  "scripts": {
  "test": "mocha 'test/**/*.test.js' --timeout 10000 --reporter mochawesome",
  "test-external": "mocha 'test/external/**/*.test.js' --timeout 10000 --reporter mochawesome",
  "test-controller": "mocha 'test/controller/**/*.test.js' --timeout 10000 --reporter mochawesome",
  "test-graphql": "mocha test/external/transferGraphqlExternal.test.js --timeout 10000 --reporter mochawesome",
  "start": "node server.js",
  "start-graphql": "node graphql/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@apollo/server": "^4.12.2",
    "bcryptjs": "^3.0.2",
    "body-parser": "^2.2.0",
    "chai": "^6.0.1",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "graphql": "^16.11.0",
    "graphql-tag": "^2.12.6",
    "jsonwebtoken": "^9.0.2",
    "mocha": "^11.7.1",
    "sinon": "^21.0.0",
    "supertest": "^7.1.4",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "mochawesome": "^7.1.3"
  }
}
